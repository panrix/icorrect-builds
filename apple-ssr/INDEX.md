# apple-ssr

**State:** active
**Owner:** parts
**Purpose:** Research and build artifacts for automating Apple Self Service Repair parts lookup, order-history analysis, and (eventual) order placement against `selfservicerepair.eu`.
**Last updated:** 2026-05-02 08:30 UTC

## Current state

### In flight
- API discovery complete (see `docs/discovery.md`); parts catalog built for 23 device models / 6,438 parts.
- Outstanding ideas tracked in `idea-inventory.md`: probe add-to-cart for live stock behavior; wire API order automation after canary-order approval; cross-join with Mobilesentrix for cheapest-path-per-repair; wrap full builder in weekly cron.

### Recently shipped
- 2026-04-29 — `parts-catalog.json` + `parts-catalog-summary.md` generated by `scripts/build-catalog.py` / `scripts/build-catalog-phase4.py`.
- 2026-04-26 — order-history audit (`docs/audits/order-history-audit.md`) covering 91 orders, GBP 11,775.10 spend.

### Next up
- Decide whether to push parts-catalog regen to a weekly cron.
- Cross-reference with mobilesentrix discovery once that flow is firmed up.

## Structure

- `briefs/` — empty; no proposals yet.
- `decisions/` — empty; backfill in Phase 7c if useful.
- `docs/` — API discovery + reference doc + parts-catalog summary; `docs/audits/` holds the order-history audit.
- `archive/` — empty.
- `scratch/` — empty.
- `scripts/` — Python builders for the parts catalog (`build-catalog.py`, `build-catalog-phase4.py`).
- `data/` — raw and processed JSON dumps (orders raw + detail, parts catalog, serial candidates, known-good serials).

## Key documents

- [`docs/discovery.md`](docs/discovery.md) — API discovery (endpoints, auth, stack) for selfservicerepair.eu
- [`docs/api-inventory.json`](docs/api-inventory.json) — full enumerated endpoint surface
- [`docs/parts-catalog-summary.md`](docs/parts-catalog-summary.md) — human-readable summary of the generated catalog
- [`docs/audits/order-history-audit.md`](docs/audits/order-history-audit.md) — 91-order historical spend audit
- [`scripts/build-catalog.py`](scripts/build-catalog.py) — main catalog builder
- [`scripts/build-catalog-phase4.py`](scripts/build-catalog-phase4.py) — phase-4 catalog builder
- [`data/parts-catalog.json`](data/parts-catalog.json) — generated parts catalog (~2.9MB)
- [`data/apple-ssr-orders-detail.json`](data/apple-ssr-orders-detail.json) — full order-history dump (~17MB)

## Open questions

- Probe add-to-cart to confirm live stock behavior (idea `51aa1b56`, P2).
- Wire API order automation after a canary order is approved (idea `ac02af63`, P2).
- Join this catalog with Mobilesentrix to find the cheapest path per repair (idea `e416abfa`, P3).
- Re-run the full builder weekly via cron once wired (idea `70a89d07`, P3).
