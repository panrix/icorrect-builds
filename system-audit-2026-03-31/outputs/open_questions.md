# Open Questions

- Is Back Market API auth currently live and fully usable for read-only discovery, or are some routes gated behind seller/session context?
- Which Monday automations are native Monday rules versus external webhook consumers on the VPS or n8n?
- What is the live split today between VPS-hosted automation and n8n-hosted automation?
- Which platforms with credentials are active production dependencies versus dormant or future-facing credentials?
- How much of the customer communication chain is in Intercom versus email, Slack, or Back Market portal messaging?
- Are there any Shopify purchase paths that bypass the documented n8n -> Monday item-creation flow?
- Should the archived `payment_reconciliation.py` logic be restored, replaced, or explicitly retired, given that no live runtime owner for Stripe/SumUp -> Monday/Xero reconciliation has been found?
- Should the parked Royal Mail API credentials be retired from the canonical env while browser automation remains the live shipping path?
- Why does the `Jarvis` Shopify custom app show zero visible subscriptions while a live `orders/create` n8n workflow exists under `shopifyOAuth2Api / Shopify account 2`?
- Is the Shopify -> Intercom native sync intentionally active alongside the n8n Shopify order flow, and if so which system owns field precedence?
- Are Intercom inboxes, macros/saved replies, and bot/routing admin surfaces exposed only in another API/UI layer for this workspace, given that `/inboxes` is `404` and `/macros` is version-blocked on the current REST surface?
- Is `reconcile-listings.js` actively run anywhere outside user crontab, or is listings reconciliation still operator-driven?
- Which system is the intended canonical scheduled sell-side owner now: `backmarket/scripts/buy-box-check.js` or `buyback-monitor/buy_box_monitor.py`?
- Which env file is actually canonical today: `/home/ricky/config/.env` or `/home/ricky/config/api-keys/.env`, given that overlapping credentials diverge and live access differs by file?
- Should `reconcile-listings.js` remain an explicit operator-controlled check, or should it become a scheduled guardrail before buy-box management?
- Should `SHOPIFY_ACCESS_TOKEN` be reduced to true read-only scopes, or is the current write-capable token intentionally shared?
- Is the n8n `Shopify account 2` OAuth credential bound to the same `Jarvis` Shopify app or to a different Shopify app installation?
- Why do staged BM payout/current-state docs still describe `buyback_payout_watch.py` as active fallback when current runtime evidence points to `bm-payout.service` as the sole live payout executor?
- Which process owns payment-received state back into Monday now that Xero is live, cash-basis, and current but the documented payment-received webhook is still undeployed?
- If Xero payment receipt is restored, what should the correct live Monday write-back be:
  - `Invoice Status = Paid`
  - `Payment Status = Confirmed` or another current label
  - `Payment Method = Invoiced - Xero`
  - `Payments Reconciled = Complete`
- Who owns the ghost-invoice cleanup backlog described in the current operations workspace, and is that owner in finance, operations, or a rebuild project?
- What is the target disposition for the `78` `REVIEW NEEDED - no payment found` ghost-invoice items in `xero_invoice_crossref.csv`?
- How do Stripe, SumUp, and bank-entry flows reach Xero in practice today: direct feed, manual import, bank rule, or mixed operator handling?
- Should the active Xero invoice workflow remain in n8n in its current embedded-secret form, or be rebuilt onto a single canonical credential path before further finance automation is added?
- Should the archived send-invoice and payment-received Xero workflows be repaired and deployed, or explicitly retired in favour of a different finance-state closure design?
- Are the historically documented Stripe-to-Xero CSV import scripts still the real import path, or has that been replaced by bank feed or accountant-driven reconciliation?
- Who owns cleanup of the current operations finance docs that still point to archive-only Xero/reconciliation scripts?
- What are `FERRARI_XERO_CLIENT_ID` and `FERRARI_XERO_CLIENT_SECRET` intended for: alternate OAuth app, legacy path, or still-needed finance access?
- What is the canonical current team-owner map for each live workflow, given that `/home/ricky/kb/team/roster.md` supersedes older February team docs and Adil is now dismissed?
- Who now owns front-desk and intake work that older team audits attributed to Adil?
- What should be treated as the canonical customer-response metric: Ferrari audit response time, live Intercom human reply time, or a channel-split metric by ingress source?
- What is the actual current median time from `Received` to `Diagnostic Complete` by client path and technician?
- What is the actual current median time from `Under Repair` to `Date Repaired` by client path and technician?
- Which elapsed-time states should be excluded from operational SLA metrics: `Repair Paused`, `Client To Contact`, courier/return states, or all customer-wait states?
- What should the concrete archive/closure policy be for the `440` open Monday items older than `366` days, and which statuses should be treated as operational debt rather than live queue?
- Which owner should be accountable for ageing in the long-tail post-diagnostic exception set, especially the `75` open items with `Diag. Complete` but no `Quote Sent` date?
- What is the best current channel-level contribution model once labour time, parts, shipping, returns, and payment-collection delay are applied to BM, Shopify, walk-in, and corporate work?
- How much of the business loss is caused by conversion failure versus margin failure versus delayed/stuck cash realisation?
- Does the current Intercom response-time improvement seen in the recent sample hold across a full month once all pages/channels are pulled, or is it being flattered by sample composition?
- What is the true quote-approval/payment lag after `Quote Sent` by path, now that broad `Diagnostic Complete -> Quote Sent` timing appears relatively healthy?
- Should Monday quote/payment date fields be treated as operationally contaminated for lag analysis until Xero/payment write-back is rebuilt, given that the `2026-04-02` proxy pull found `0` recent quoted items with a populated `Xero Invoice ID` and many payment dates preceding `Quote Sent`?
- For quote-decision timing, should the canonical next-state set be treated as:
  - `Invoiced`
  - `Quote Rejected`
  - `Queued For Repair` when invoicing status was skipped
- Is there another Monday history/export path for quote/comms events, given that the accessible `status4` activity-log surface yielded only `3` usable quote-to-next-decision traces out of `126` recent quoted items and the live board has no visible `comms_status` column?
- Is there a cleaner Monday history source that can join free-text updates to historical status-at-update-time, so blocker categories can be measured against actual resolution duration?
- Should `Systems Manager` template updates remain in the main board, or be moved to a less noisy structured log surface now that they dominate the update corpus?
- Is there a formal rule for what staff should document in Monday updates versus Intercom notes versus status changes alone?
- Is there a device/model field or linked-board export that can be joined cleanly to technician completions, given that the current `Device` relation text surface comes back blank in the staff pass?
- Is Michael Ferrari's activity history truncated by the accessible Monday log cap, or is his extremely low completion count in this window genuinely representative of current bench output?
- Which value field should be treated as canonical for technician revenue attribution: `dup__of_quote_total`, Xero invoice amount, or another Monday field?
- Is there a current owned source that ties enquiry origin and paid-job closure together across Typeform, Intercom, Shopify, phone, and walk-in demand?
- What should the rebuilt target-state payment truth be under cash accounting once the broken current-state loops are replaced?
- Should the current Monday `Source` field be rehabilitated, or replaced with a new mandatory ingress-source capture so phone/direct demand stops disappearing from attribution?
- What should the rebuilt supplier source-of-truth look like, given that Xero is currently the best available source but is still not operationally sufficient?
- How should Monday's canonical customer identity be normalised into a stable reusable customer ID rather than email/phone/name heuristics?
- What is the lowest-friction operational way to capture actual bench occupancy separately from technician queue ownership?
