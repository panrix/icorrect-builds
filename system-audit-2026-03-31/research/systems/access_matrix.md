# Access Matrix

| System | Credential / Hint | Access Method | Verification Result | Scope / Notes | Status |
|---|---|---|---|---|---|
| Monday.com | `MONDAY_APP_TOKEN` | GraphQL API `https://api.monday.com/v2` | `Observed`: reachable via `query { me { id name } }` | non-personal app token; main board `349212843`, BM board `3892194968` documented | Complete for auth, discovery pending |
| Intercom | `INTERCOM_API_TOKEN` | REST API `https://api.intercom.io/admins` and related list/search endpoints | `Observed`: reachable, HTTP 200 | workspace ID `pt6lwaq6`; live inventory pull returned `3` admins, `1` team, `1` visible inbox seat, `21` tags, `3` segments, `57` contact attributes, `30,650` tickets, `169` companies, `1` help center with `website_turned_on=false`, `0` articles, and `0` collections; `/inboxes` returned `404`, `/macros` returned `intercom_version_invalid`, `/news/news_items` returned `404` | Partial inventory complete |
| Shopify | `SHOPIFY_ACCESS_TOKEN`, `SHOPIFY_STORE` | REST + GraphQL Admin API | `Observed`: reachable, HTTP 200 | current app installation resolves as `Jarvis`; shared docs describe this token as read-only, but live `/admin/oauth/access_scopes.json` and `currentAppInstallation.accessScopes` show write scopes including `write_orders`, `write_products`, `write_content`, `write_theme_code`, and `write_themes`; live inventory returned `967` products, `109` orders, `182` customers, `16` themes, and `0` visible webhook subscriptions on both GraphQL and REST for this app token; recent paid order samples show `payment_gateway_names=[\"shopify_payments\"]`; `shopifyPaymentsAccount` exists but is scope-blocked without `read_shopify_payments`; active order-ingress workflow uses an n8n `shopifyTrigger` node with Shopify OAuth credentials, which now points more strongly to a different app/credential owner than the `Jarvis` custom app token; shipping zones/pages/blogs/metafields are readable; `price_rules`, `script_tags`, `locations`, and `product_listings` are scope-gated; `appInstallations` access denied | Partial inventory complete |
| Supabase | `SUPABASE_URL`, `SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY` | REST API `/rest/v1/` | `Observed`: reachable, HTTP 200 | shadow/app data layer; service-role use should be minimized | Complete for auth, discovery pending |
| n8n Cloud | `N8N_CLOUD_API_TOKEN` | REST API `/api/v1/workflows` | `Observed`: reachable, HTTP 200 | cloud host `icorrect.app.n8n.cloud` documented | Complete for auth, discovery pending |
| Back Market | `BACKMARKET_API_AUTH`, `BACKMARKET_API_BASE`, `BACKMARKET_API_LANG`, `BACKMARKET_API_UA` | BM web API | `Observed`: reachable via `GET /ws/orders?state=1` | all three headers required; docs warn wrong locale defaults can alter behavior | Complete for auth, discovery pending |
| Slack | `SLACK_BOT_TOKEN`, `SLACK_USER_TOKEN`, `SLACK_APP_TOKEN`, `SLACK_AUTOMATIONS_BOT_TOKEN` | Slack Web API / socket mode | `Observed`: reachable via `auth.test` | likely shared between internal comms and automations | Complete for auth, discovery pending |
| Xero | `XERO_CLIENT_ID`, `XERO_CLIENT_SECRET`, `XERO_REFRESH_TOKEN`, `XERO_TENANT_ID` | OAuth + REST API | `Observed`: reachable via `/connections`, `/Organisation`, `/Accounts`, `/Invoices`, `/Payments`, `/BankTransactions`, `/Contacts`, and live reporting endpoints | tenant `Panrix Ltd`; cash VAT basis (`SalesTaxBasis=CASH`, quarterly); `135` accounts; live current-period `ACCREC`, `ACCPAY`, payments, bank transactions, contacts, and April 2026 P&L/Balance Sheet confirmed; refresh token rotates on every call and must continue to be refreshed through the persistence script | Partial inventory complete |
| PostHog | `POSTHOG_API_KEY`, `POSTHOG_PROJECT_ID` | PostHog API | `Observed`: reachable via project metadata endpoint | website/theme code references PostHog tracking heavily | Complete for auth, discovery pending |
| Google APIs | `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `JARVIS_GOOGLE_REFRESH_TOKEN`, `EDGE_GOOGLE_REFRESH_TOKEN` | OAuth + Google APIs | `Observed`: partially reachable | `config/.env` Jarvis refresh token successfully exchanged and can read Search Console (`1` site entry) plus Calendar (`1` calendar list item), but Drive list returns `403 insufficientPermissions`; `EDGE_GOOGLE_REFRESH_TOKEN` is missing from `config/.env` yet present and reachable in `config/api-keys/.env`, where it can read Search Console (`2` site entries) and Drive (`1` sampled file) | Partial inventory complete; env drift present |
| Typeform | `TYPEFORM_API_TOKEN` | Typeform API | `Observed`: canonical env token failed, legacy env token works | `config/.env` token returned HTTP `403` `AUTHENTICATION_FAILED` on `GET /forms?page_size=1`; `config/api-keys/.env` token returned HTTP `200` and listed `37` forms, including `iCorrect Walk-In (v2)` | Partial inventory complete; env drift present |
| Cloudflare | `CLOUDFLARE_API_TOKEN` | Cloudflare API | `Observed`: token verify returned HTTP 401 | token present but invalid/revoked/wrong scope for tested endpoint; local docs now show status notifications cut over to the VPS on `2026-04-01`, so Cloudflare is mainly a legacy/frontend ownership question until valid API access is restored | Blocked |
| Stripe | `STRIPE_API_KEY` | Stripe API | `Observed`: reachable via `GET /v1/account` and `GET /v1/payment_intents?limit=3` | live account `acct_1P8Ks7S1IqGUbO9S`, `GB`, `gbp`, charges/payouts enabled; sampled payment intents are `succeeded` and carry invoice-style metadata (`Invoice number`, `EmailAddress`, `OrgCode`, `OrgName`) | Partial inventory complete |
| SumUp | `SUMUP_API_KEY` | SumUp API | `Observed`: canonical env key failed, legacy env key works | `config/.env` key returned HTTP `401` on `GET /v0.1/me`; `config/api-keys/.env` key returned HTTP `200` with merchant profile `Panrix Ltd T/a iCorrect` | Partial inventory complete; env drift present |
| Telegram | `TELEGRAM_BOT_TOKEN` | Bot API | `Observed`: reachable via `getMe` | likely used for alerts and agent notifications | Complete for auth, discovery pending |
| JARVIS email | `JARVIS_EMAIL`, `JARVIS_EMAIL_PW`, IMAP/SMTP vars | IMAP/SMTP | `Observed`: reachable with config drift | IMAP login to `uk1000.siteground.eu:993` succeeded and `INBOX` select returned `OK`; configured SMTP path `uk1000.siteground.eu:465` times out, but the same credentials successfully authenticate over `587` and `2525` with `STARTTLS` | Partial inventory complete; SMTP config drift present |
| SICKW | `SICKW_API_KEY` | device info API | `Observed`: reachable via sample iCloud lookup probe | used in icloud-checker flow per BM process docs | Complete for auth, discovery pending |
| Royal Mail | `ROYAL_MAIL_CLIENT_ID`, `ROYAL_MAIL_CLIENT_SECRET` | Royal Mail tracking API | `Observed`: API path appears intentionally parked | tested OAuth token request against `https://api.royalmail.net/oauth/token` returned a `404 Not Found` body rather than an access token; local BM docs say Royal Mail API onboarding/subscription is parked and browser automation remains the live path | Partial inventory complete; API parked |
| n8n Self-Hosted | `N8N_API_TOKEN` | REST API `https://n8n.icorrect.co.uk/api/v1/workflows` | `Observed`: reachable, HTTP 200 | reachable but current inventory appears nearly dormant | Complete for auth, discovery pending |
