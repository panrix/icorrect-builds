# Xero Invoice Automation

This folder is a pointer to a repo-owned finance workflow workspace.

Canonical source:

- GitHub: `git@github.com:panrix/xero-invoice-automation.git`
- Current source branch: `main`
- Preserved sanitized source commit: `7f73e71`

Parent-repo rule:

- `panrix/icorrect-builds` does not track Xero workflow source files here.
- Keep the real checkout as its own Git repository, then move it to `~/finance/xero-invoice-automation/` during the Phase 7b folder migration.
- Historical local workflow files contained live-looking token material, so GitHub was populated from a sanitized clean snapshot rather than the old local Git history.

Local archive:

- Original local folder was archived at `/Users/ricky/vps/archive/2026-05-04-nested-repo-originals/xero-invoice-automation-original`.

