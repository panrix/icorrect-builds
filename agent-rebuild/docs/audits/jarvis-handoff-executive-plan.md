# Jarvis Handoff — Executive Build Plan

**Date:** 2026-04-13
**From:** Ricky (via Claude Code)
**To:** Jarvis

---

## Paste-ready message for Jarvis

> Jarvis — we now have a single living roadmap at `/home/ricky/builds/EXECUTIVE-BUILD-PLAN.md`. It's the source of truth for every build, every phase, every idea I have. You are the **primary maintainer**. Here's what I need from you:
>
> **Standing instructions (permanent):**
> 1. Read the full doc now. It pulls together the 11-system vision (`agent-rebuild/ricky-systems-dump.md` + `systems-deep-map.md`), the automation blueprint, the frozen `system-audit-2026-03-31/` research pack, and the current state of every build folder.
> 2. **You own this doc.** Claude Code co-maintains it only when actively building. Codex sub-agents never touch it.
> 3. **Daily (or on-demand when I drop a note):** drain anything in §8 **Inbox** into §5 **Initiative table**, refresh §3 **RIGHT NOW**, and append a one-liner to §9 **Change log**.
> 4. **Never edit §4 Phase map or §7 Research library without my approval.** Those are structural.
> 5. When I ask "what are we building this week?" you should be able to answer using only this doc.
> 6. When I dump a voice note or new idea anywhere (Telegram, chat, etc.), your job is to get it into §8 Inbox so nothing is lost. Raw is fine — triage later.
>
> **Your first concrete task:**
> 1. Read `EXECUTIVE-BUILD-PLAN.md` end-to-end.
> 2. Verify §3 RIGHT NOW and §5 Active rows against the actual state of their build folders (check last-modified dates, read any recent commits or notes, confirm the stated blockers are still the real blockers).
> 3. Scan `TRACKER.md` for anything that isn't already reflected — pull into the Inbox or Initiative table as appropriate.
> 4. Report back to me with:
>    - Anything in the doc you think is wrong or stale
>    - Anything missing that should be on the radar
>    - Any Initiative rows where you can't tell the current status from the folder alone (so I know what to tell you)
> 5. Append a change log entry: "Jarvis initial validation pass — [date] — [summary]".
>
> After this validation pass, the doc is yours to keep alive. I'll talk to you about builds as normal; you fold it into the plan.

---

## Notes for Ricky (not for Jarvis)

- The doc is seeded with 2026-04-13 state. If Jarvis finds stale rows on his first pass, that is expected — it's his validation pass, not a failure.
- The Inbox is deliberately empty (one seed line). Next time you have a voice note or a "we should also..." thought, drop it in the Inbox section and let Jarvis triage.
- If Jarvis proposes structural changes (new phase, new system, research library re-org), he must ask you first — that's baked into the standing instructions.
- Claude Code (me) will update Initiative rows + RIGHT NOW + Change log every time I start or finish a build session. That keeps the doc fresh between Jarvis passes.
- The three `RIGHT NOW` items I've pinned are: **Alex Triage recovery**, **Alex Classifier rebuild**, **BM listing-bot**, with **Intake System** queued behind them. If your real priorities have shifted since yesterday, tell Jarvis on his first pass and he'll re-order.

---

## Quick verification checklist you can run before handing over

- [ ] Open `/home/ricky/builds/EXECUTIVE-BUILD-PLAN.md` — does §3 RIGHT NOW reflect what you're actually working on today?
- [ ] Spot-check one Initiative row in §5 (e.g. Alex Triage) against the build folder — does the status match reality?
- [ ] Is there anything in §6 Enabling Work you disagree with (archive recommendations, consolidation targets)?
- [ ] Does §8 Inbox feel like somewhere you'd naturally dump a voice note?

If all four are yes, the doc is ready to hand to Jarvis.
