# Elek Board Viewer

This folder is a pointer to a repo-owned diagnostics workspace.

Canonical source:

- GitHub: `git@github.com:panrix/elek-board-viewer.git`
- Current source branch: `main`
- Preserved clean source commit: `22c87df`

Parent-repo rule:

- `panrix/icorrect-builds` does not track Elek board-viewer source files here.
- Keep the real checkout as its own Git repository, then move it to `~/diagnostics/elek-board-viewer/` during the Phase 7b folder migration.
- Large schematic/reference/runtime assets are intentionally outside normal Git for now.

Local archive:

- Original local folder with large runtime/schematic assets was archived at `/Users/ricky/vps/archive/2026-05-04-nested-repo-originals/elek-board-viewer-original`.

