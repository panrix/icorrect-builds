# Intake React Form

This folder is a pointer for the former nested intake React form workspace.

Canonical source:

- Decision: fold source into parent-owned `intake-system/frontend`.
- Former nested repo branch during cleanup: `master`
- Former parent gitlink commit: `9d65f3dfc958e50ed3a584becc0abb12625eab83`

Parent-repo rule:

- `panrix/icorrect-builds` does not track React form source files here.
- Do not create a dedicated GitHub repository for this path unless the Intake plan changes.
- Preserve source by folding it into `intake-system/frontend` when the active Intake session is quiet.
- Do not copy generated `dist/` output into the parent repo.
