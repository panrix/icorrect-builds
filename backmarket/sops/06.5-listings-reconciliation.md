# SOP 6.5: Listings Reconciliation

**Version:** 1.0
**Date:** 2026-03-22
**Scope:** Daily reconciliation between Monday boards and Back Market active listings. Ensures data integrity before buy box management (SOP 7) runs.
**Owner:** Backmarket agent (Hugo) / Jarvis

---

## Trigger

- Daily cron, runs AFTER V7 scraper and BEFORE buy box check (SOP 7)
- On-demand when requested

**Run order (after scraper has run):**
1. V7 sell price scraper (weekly — see `buyback-monitor/run-weekly.sh`)
2. **SOP 6.5: Listings reconciliation** ← this SOP
3. SOP 7: Buy box management

---

## Step 1: Gather data from both sources

### 1a: Monday — all "Listed" items
Query Main Board (349212843), group "BMs Awaiting Sale" (`new_group88387__1`), filter `status24` = Listed (index 7).

For each item, read:
- Item ID, name
- `status24` (Repair Type / listing status)

### 1b: BM — all listings
`GET /ws/listings` paginated. For each listing:
- `listing_id`, `quantity`, `grade`, `title`, `price`, `sku`, `product_id`

### 1c: BM Devices Board — linking data
For each Monday "Listed" item, find the corresponding BM Devices Board (3892194968) entry via board_relation (`... on BoardRelationValue { linked_item_ids }`).

Read:
- `text_mkyd4bx3` (stored listing ID)
- `numeric_mm1mgcgn` (Total Fixed Cost)
- `numeric` (Purchase Price)

---

## Step 2: Cross-reference and identify mismatches

### Check A: Monday Listed → BM Active
For each Monday "Listed" item:
1. Get its listing ID from BM Devices `text_mkyd4bx3` (single source of truth)
2. Find that listing_id in BM active listings
3. If found and qty > 0: ✅ matched
4. If found but qty = 0: ⚠️ Listed on Monday but offline on BM
5. If not found: ⛔ Listed on Monday but no matching BM listing

### Check B: BM Active → Monday Listed
For each BM active listing (qty > 0):
1. Search BM Devices Board for items with `text_mkyd4bx3` matching the listing_id
2. Find the linked Main Board item via `board_relation`
3. Check Main Board `status24`:
   - If "Listed" (7): ✅ matched
   - If "Sold" (10): ⛔ **OVERSELL RISK** — device sold but still live on BM
   - If "To List" (8): ⚠️ BM active but Monday not updated
   - If any other status: ⛔ **OVERSELL RISK** — device not in listable state
   - If no Monday item found: ⛔ **ORPHAN LISTING** — active on BM with no Monday tracking

### Check C: Quantity reconciliation
For each active `listing_id`:
1. Count Monday "Listed" items that map to this listing
2. Compare to BM listing qty
3. If Monday count > BM qty: ⚠️ missed revenue (more devices than listed)
4. If BM qty > Monday count: ⛔ **OVERSELL RISK** (listing shows more than we have)

### Check D: Missing BM Devices entry
For each Monday "Listed" item:
1. Check if a BM Devices Board entry exists (via board_relation)
2. If no entry: ⛔ **MISSING BM DEVICE** — cannot track listing ID, costs, or profitability

### Check E: Missing cost data
For each BM Devices entry linked to an active listing:
1. Check `numeric_mm1mgcgn` (Total Fixed Cost) is present and > 0
2. If missing: ⚠️ flag for backfill
3. If Main Board link exists: auto-calculate from purchase + parts + labour + shipping + buy fee
4. If no Main Board link: flag for manual entry

---

## Step 3: Automated actions (safety only)

**Auto-take offline (no approval needed):**
- BM listing active but Monday status = "Sold", "Shipped", "BM RTN/RFD", or any non-Listed status
- BM qty > Monday device count for that spec

**Auto-fix (no approval needed):**
- Missing Total Fixed Cost where data can be calculated from Monday columns

**Flag only (needs review):**
- Monday says Listed but BM listing is offline/missing
- Monday count > BM qty (missed revenue)
- Missing BM Devices entry
- Orphan BM listings (no Monday match)

---

## Step 4: Report

Post to BM Telegram (`-1003888456344`):

```
📋 Listings Reconciliation — {date}

Monday Listed: {X} | BM Active: {Y} | Delta: {X-Y}

✅ Matched: {count}
⛔ Oversell risk (auto-offlined): {count}
⛔ Qty oversell (offlined): {count}
⚠️ Monday Listed but BM offline: {count}
⛔ No listing found: {count}
⛔ Orphan BM listings: {count}
⛔ Spec mismatch: {count}
⚠️ Missing cost data: {count}
⛔ Missing BM Device entry: {count}
⚠️ Missed revenue (more devices than listed): {count}
✅ Cost auto-backfilled: {count}
```

---

## Step 5: Missing BM Device Protocol

When a Monday "Listed" item has no BM Devices Board entry:

1. **Flag the item for manual review**
2. **Do NOT auto-create** a BM Devices item in the current implementation
3. **Do NOT auto-list**. The device needs to go through SOP 6 properly after a BM Devices entry is created manually
4. Include it in the reconciliation report under `Missing BM Device entry`

---

## Columns used

### Main Board (349212843)
| Column ID | Purpose |
|-----------|---------|
| `status24` | Listing status (Listed=7, To List=8, Sold=10) |
| `lookup_mkx1xzd7` | Parts cost mirror (comma-separated, summed for cost backfill) |
| `formula_mkx1bjqr` | Labour cost £ (readable reference, not used in formula) |
| `formula__1` | Labour hours |

> Listing ID is NOT on the Main Board. Single source of truth is BM Devices `text_mkyd4bx3`.

### BM Devices Board (3892194968)
| Column ID | Purpose |
|-----------|---------|
| `text_mkyd4bx3` | Listing ID |
| `numeric_mm1mgcgn` | Total Fixed Cost |
| `numeric` | Purchase Price |
| `status__1` | RAM |
| `color2` | SSD |
| `board_relation` | Link to Main Board (use `... on BoardRelationValue { linked_item_ids }`) |

---

## Error handling

- Monday API failure: abort reconciliation, alert. Do not run SOP 7 without reconciliation.
- BM API failure: abort, alert.
- If reconciliation finds oversell risks: resolve those BEFORE running SOP 7.

---

## What does NOT happen

- No relisting without going through SOP 6
- No cost guessing — either calculate from real data or flag
- No qty changes without physical verification

---

## Future additions (noted Mar 22)

### Remediation steps
Each issue type needs defined next steps. Currently flagged but remediation is manual. Need to document:
- Monday Listed / BM offline → check why, relist via SOP 06 or update Monday status
- No listing ID → set status to To List, SOP 06 picks up
- Missing BM Device → create entry (Step 5), set to To List
- Orphan listing → investigate, link to Monday or take offline

### BM Devices board group verification
Script should verify that active BM Device items are in the "BM To List / Listed / Sold" group (`new_group`). Items in wrong groups (e.g. "BM Returns") should be flagged. A daily cron to check group placement would catch items that get stuck after returns are processed.

---

## QA Notes (2026-03-28)

This section records QA of SOP 6.5 against the active script at `backmarket/scripts/reconcile-listings.js`.

### Findings

1. **HIGH — Step 5 "Missing BM Device Protocol" is documented but not implemented**

The SOP says that when a Monday "Listed" item has no BM Devices entry, the script should:
- create a BM Devices item
- link it to Main Board
- populate purchase price/specs
- send a Telegram note

The script does not do that. It only flags the item into `results.missingBmDevice`.

**Script reference:** `reconcile-listings.js` Check A missing BM Device handling.

2. **MEDIUM — Check C implementation does not match the SOP wording exactly**

The SOP says Check C is per `product_id + grade` combination.

The script actually reconciles quantity by `listing_id`, using the set of matched devices for that listing. That may be operationally acceptable, but it is not what the SOP says.

**Script reference:** quantity reconciliation block.

3. **MEDIUM — Telegram report is not as detailed as the SOP example**

The script now sends a Telegram summary after every run, which fixes the missing report bug.

However, the Telegram message does **not** include per-listing/per-device detail lines in the way the SOP example suggests. It sends counts only, with some additional categories not shown in the SOP example:
- `No listing found`
- `Orphan BM listings`
- `Spec mismatch`
- `Cost auto-backfilled`

This is a documentation mismatch, not a runtime bug.

4. **LOW — Columns used section is incomplete**

The script also uses these columns during reconciliation, but they are not listed in the "Columns used" section:
- Main Board: `lookup_mkx1xzd7`, `formula_mkx1bjqr`, `formula__1`
- BM Devices: `status__1`, `color2`

The IDs already appear elsewhere in the SOP, so this is a completeness/documentation issue rather than a logic bug.

### Check Summary

| Check | Status | Notes |
|------|--------|-------|
| Do Checks A-E match the script? | MOSTLY PASS | A-E exist and run, but Check C is by `listing_id` not `product_id + grade`, and Step 5 remediation is not implemented |
| Are both auto-offline actions implemented? | PASS | Check B oversell and Check C qty oversell both set BM qty to `0` |
| Does Telegram format match the SOP? | MOSTLY PASS | Report sends every run, but summary is count-based rather than detailed example format |
| Are column IDs correct against the script? | MOSTLY PASS | Core IDs are correct; "Columns used" section omits some actively-used fields |
| Is cost backfill formula correct? | PASS | `purchase + parts + labour + shipping + buy fee` |
| Remaining dead code/stale refs? | PASS WITH NOTES | Dead helper removed; no stale output path; Step 5 protocol text is still ahead of implementation |

### Confirmed Behaviors

- **Check A:** Implemented. Monday Listed items are matched via BM Devices `text_mkyd4bx3`, then checked against BM listing presence/qty. Spec verification checks RAM and SSD in title. Grade check is intentionally skipped here.
- **Check B:** Implemented. Active BM listings are cross-checked back to Monday item status, and non-Listed states become oversell risk.
- **Check C:** Implemented. Qty mismatches are identified, including BM qty > Monday count oversell risk.
- **Check D:** Implemented as flagging only. Missing BM Devices entries are detected and reported.
- **Check E:** Implemented. Missing `numeric_mm1mgcgn` triggers auto-backfill when Main Board data exists.

### Auto-actions Verified

The script implements both safety auto-offline paths:
- **Check B oversell:** active BM listing with wrong Monday status → `POST /ws/listings/{id}` with `quantity: 0`
- **Check C qty oversell:** BM qty greater than Monday count → `POST /ws/listings/{id}` with `quantity: 0`

### Cost Backfill Verified

The backfill formula in the script is:

```text
fixed = purchase + parts + labour + shipping + buyFee
```

Where:
- `parts` comes from Main Board `lookup_mkx1xzd7`
- `labour` is `formula__1 × £24`
- `shipping` is `£15`
- `buyFee` is `purchase × 10%`

This matches the SOP.

### Known Remaining Gaps (documented, non-blocking)

- No `--dry-run` mode. The script always executes auto-actions.
- Grade check is skipped in Check A spec verification.
- Check B reads Monday item status item-by-item, which is slower but functional.

### Syntax Check

`node --check backmarket/scripts/reconcile-listings.js` passed during QA.
