# Backmarket Handoff

**Date:** 24 Mar 2026
**Scope:** Current state at end of day for BM rebuild work

## Current Status

- `bm-payout` is deployed live at `127.0.0.1:8012` behind nginx at `/webhook/bm/payout`. Local and public POST smoke tests return `OK`; real Monday webhook validation is still pending.
- `bm-shipping` is deployed live at `127.0.0.1:8013` behind nginx at `/webhook/bm/shipping-confirmed`. It hard-gates missing serial and uses Main Board `board_relation5` directly.
- `bm-grade-check` is deployed live at `127.0.0.1:8011` behind nginx at `/webhook/bm/grade-check`. It uses the schema-correct formula/mirror fields and typed fragments.

## Monday Schema Discoveries

- Main Board `board_relation5` links to **BM Devices Board** (3892194968). The monolith confirms this at index.js:1540 and `getBmClaimedSpecs()` successfully reads BM Devices columns from linked items.
- BM Devices Board `board_relation` links **back** to Main Board (349212843). This is a separate column on a different board — not the same relation.
- Main Board `formula__1` and `formula_mkx1bjqr` must be read as `FormulaValue.display_value`.
- Main Board `lookup_mkx1xzd7` must be read as `MirrorValue.display_value` and summed when it returns comma-separated values.
- `FormulaValue`, `MirrorValue`, and `BoardRelationValue` must use typed fragments. Plain `.text` / `.value` is not reliable for those fields.

> **Correction (QA, 24 Mar):** The original handoff claimed `board_relation5` links to "Devices Board, not BM Devices." This is wrong. The monolith uses `board_relation5` to read BM Devices columns (`status__1`, `color2`, `status7__1`, `status8__1`) and this works in production. bm-shipping's reverse-lookup approach via `findBmDeviceByMainItemId()` is correct but slower than reading `board_relation5` directly.

## Fixed Today

- `bm-shipping` now hard-blocks if Main Board `text4` (serial) is missing.
- `bm-shipping` reads `board_relation5` directly from Main Board to find linked BM Devices item (O(1) lookup, matching the monolith's approach).
- `bm-shipping` throws on Monday HTTP/GraphQL errors instead of silently accepting bad responses.
- `bm-grade-check` now reads the confirmed formula and mirror fields correctly, uses typed fragments (`FormulaValue`, `MirrorValue`), and sums comma-separated parts cost.
- `bm-grade-check` extracts UUID from BM Devices item (`text_mm1dt53s`) for model matching (was broken — passed null).
- `bm-grade-check` throws on Monday HTTP/GraphQL errors.
- `bm-payout` throws on Monday HTTP/GraphQL errors (was silent).

## Remaining Work

- ~~Test with real Monday webhook triggers.~~ ✅ Done 26 Mar
- ~~Remove monolith handlers once verified.~~ ✅ Done 26 Mar (1994 → 1428 lines)
- Verify "Return Booked" status label exists on Monday for dispatch.js.
- Confirm sale-detection cron schedule if it needs to be installed or adjusted.

## Completed

1. ✅ Deploy and cut over bm-payout (8012), bm-shipping (8013), bm-grade-check (8011).
2. ✅ Verify the three split routes with real Monday webhook triggers (26 Mar).
3. ✅ Remove extracted handlers from icloud-checker monolith (26 Mar).

## Still Pending

1. Verify dispatch.js "Return Booked" works with a real label purchase.
2. Set up sale-detection.js cron schedule if not already running.
