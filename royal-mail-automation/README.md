# Royal Mail Automation

This folder is a pointer to a repo-owned operations fulfillment workspace.

Canonical source:

- GitHub: `git@github.com:panrix/royal-mail-automation.git`
- Current source branch: `main`
- Preserved source commit: `5608e90`

Parent-repo rule:

- `panrix/icorrect-builds` does not track Royal Mail automation source files here.
- Keep the real checkout as its own Git repository, then move it under `~/operations/royal-mail-automation/` during Phase 7b.
- Runtime dependency folders such as `node_modules/` are not source of truth.
