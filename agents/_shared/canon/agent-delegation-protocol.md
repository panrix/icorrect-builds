# Agent Delegation Protocol

> STUB — full protocol finalised in Phase 1b after queue schemas are locked.

Queue schema references reserved by the plan:

- Envelope: `_shared/monday/schemas/v1/queue-record.schema.json`
- Project payload: `_shared/monday/schemas/v1/payload-project.schema.json`
- Worker payload: `_shared/monday/schemas/v1/payload-worker.schema.json`
- Agent payload: `_shared/monday/schemas/v1/payload-agent.schema.json`
- Delegation payload: `_shared/monday/schemas/v1/payload-delegation.schema.json`
- Decision payload: `_shared/monday/schemas/v1/payload-decision.schema.json`

Until Phase 1b lands, follow the build plan and do not treat this file as authoritative protocol text.
