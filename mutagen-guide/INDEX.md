# mutagen-guide

**State:** active
**Owner:** none (suggest: main or systems — VPS-side tooling reference)
**Purpose:** Standalone setup guide for mirroring a VPS to a local Mac with Mutagen over SSH. The HTML is the source artifact; the PDF is a published render.
**Last updated:** 2026-05-02

## Current state

### In flight
- No live state captured — needs population by owner agent. The guide is a finished how-to artifact; the only flagged follow-up is the `mutagen.yml` reusable-config idea (idea-inventory id `4d631c1e`).

### Recently shipped
- 2026-04-28 — PDF render generated from the HTML guide.

### Next up
- Idea captured but not actioned: add a `mutagen.yml` project file for reusable sync setup (idea `4d631c1e`).

## Structure

- `briefs/` — empty (no proposals yet).
- `decisions/` — empty.
- `docs/` — the canonical guide artifacts (HTML source + PDF render).
- `archive/` — empty.
- `scratch/` — empty.

## Key documents

- [`docs/mutagen-mac-to-vps.html`](docs/mutagen-mac-to-vps.html) — source HTML guide for Mutagen Mac↔VPS sync setup.
- [`docs/mutagen-mac-to-vps.pdf`](docs/mutagen-mac-to-vps.pdf) — published PDF render.

## Open questions

- Add a `mutagen.yml` project file for reusable sync setup? (idea-inventory `4d631c1e`)
